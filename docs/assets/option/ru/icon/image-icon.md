{{ target: image-icon }}

${prefix} type ('image')
icon content type is 'image'.

${prefix} src (string)
Image source

${prefix} shape ('circle' | 'square')
Image cropping shape

{{ use: base-icon(
    prefix = ${prefix}
) }}