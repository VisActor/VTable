{{ target: svg-иконка }}

${prefix} тип ('svg')
The иконка content тип is 'svg'.

${prefix} svg (строка)
The content из svg. Supports svg path или url.

{{ use: base-иконка(
    prefix = ${prefix}
) }}