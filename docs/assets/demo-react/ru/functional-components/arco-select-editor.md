--- категория: примеры группа: функциональный-компонентs заголовок: использовать арко-дизайн выбрать компонент в ячейка редактор обложка: https://lf9-dp-fe-cms-tos.byteorg.com/obj/bit-cloud/втаблица/preview/арко-выбрать-редактор.png ссылка: редактирование/edit_cell --- # использовать арко-дизайн выбрать компонент в ячейка редактор Create a пользовательский редактор class, ArcoсписокEditor, и implement the onStart method, create an арко-дизайн выбрать компонент, и mount the компонент into the редактор контейнер. Refine the isEditorElement и onEnd методы. reference: https://visactor.io/втаблица/guide/редактирование/edit_cell https://арко.дизайн/react/компонентs/выбрать ## код демонстрация ```javascript живаядемонстрация шаблон=втаблица-react // импорт * as ReactВТаблица от '@visactor/react-втаблица'; class ArcoсписокEditor { constructor() { this.корень = null; this.элемент = null; this.контейнер = null; } onStart(editorContext) { const { контейнер, referencePosition, значение } = editorContext; this.контейнер = контейнер; this.createElement(значение); значение && this.setValue(значение); (null == referencePosition ? void 0 : referencePosition.rect) && this.adjustPosition(referencePosition.rect); } createElement(defaultValue) { const div = документ.createElement('div'); div.стиль.позиция = 'absolute'; div.стиль.ширина = '100%'; div.стиль.заполнение = '1px'; div.стиль.boxSizing = 'граница-box'; div.стиль.backgroundColor = '#232324'; this.контейнер.appendChild(div); this.корень = ReactDom.createRoot(div); const options = ['Beijing', 'Shanghai', 'Guangzhou']; this.корень.рендер( <div> <Arкодsign.выбрать placeholder="выбрать Город" defaultValue={defaultValue} onChange={значение => { this.currentValue = значение; }} > {options.map((опция, index) => ( <Arкодsign.выбрать.опция key={опция} значение={опция} classимя="арко-выбрать-втаблица"> {опция} </Arкодsign.выбрать.опция> ))} </Arкодsign.выбрать> </div> ); this.элемент = div; } getValue() { возврат this.currentValue; } setValue(значение) { this.currentValue = значение; } adjustPosition(rect) { if (this.элемент) { (this.элемент.стиль.верх = rect.верх + 'px'), (this.элемент.стиль.лево = rect.лево + 'px'), (this.элемент.стиль.ширина = rect.ширина + 'px'), (this.элемент.стиль.высота = rect.высота + 'px'); } } onEnd() { this.контейнер.removeChild(this.элемент); } isEditorElement(target) { // cascader创建时时在cavas后追加一个dom，而popup append在body尾部。不论popup还是dom，都应该被认为是点击到了editor区域 возврат this.элемент.contains(target) || this.isНажатьPopUp(target); } isНажатьPopUp(target) { while (target) { if (target.classсписок && target.classсписок.contains('арко-выбрать-втаблица')) { возврат true; } // 如果到达了DOM树的顶部，则停止搜索 target = target.parentNode; } // 如果遍历结束也没有找到符合条件的父元素，则返回false возврат false; } } const редактор = новый ArcoсписокEditor(); втаблица.регистрация.редактор('список-редактор', редактор); const опция = { defaultRowвысота: 34, колонки: [ { поле: '0', заголовок: 'имя' }, { поле: '1', заголовок: 'возраст' }, { поле: '2', заголовок: 'пол' }, { поле: '3', заголовок: 'хобби' }, { поле: '4', заголовок: 'Город', ширина: 150, редактор: 'список-редактор' } ], records: новый массив(1000).заполнить().map(() => ['Join', 18, 'мужской', '🏀', 'Shanghai']) }; const корень = ReactDom.createRoot(документ.getElementById(CONTAINER_ID)); корень.рендер(<ReactВТаблица.списоктаблица опция={опция} высота={'500px'} />); // Релиз openinula instance, do не copy window.пользовательскийРелиз = () => { корень.unmount(); }; ``` 